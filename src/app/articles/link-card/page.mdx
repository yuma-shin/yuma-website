import { ArticleLayout } from '@/components/ArticleLayout'
import { LinkCard } from '@/components/LinkCard'

export const article = {
  author: 'Yuma Shintani',
  date: '2025-02-24',
  title: '個人WEBにLink Card機能を導入した',
  description:
    'お久しぶりです。個人WEBに外部リンクをカードコンポーネントとしてレンダリングさせるリンクカード機能を導入しました。',
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

お久しぶりです。Tailwind UIが提供するテンプレートを使用したこのWEBサイトですが、やはりテンプレートのままだと少しさみしいので、
指定した外部リンクのメタデータを取得し、リンクカードとしてレンダリングさせる機能を導入しました。

紆余曲折あり、結局導入というよりは1から実装しましたが...

↓ 自動でToCを作ってくれる機能も入れました。

# 目次

# Link Cardとは？

自動的にリンク先のmatadataから`title`, `description`, `image`などの情報を取得し、カード形式でリンク先の情報を表示するものです。

<LinkCard url="http://www.y-shin.net" />
